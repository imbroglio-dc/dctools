% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replication.R
\name{compare_targets}
\alias{compare_targets}
\title{Compare computed results against documented targets at their tolerances}
\usage{
compare_targets(results, targets, tolerances = NULL, by = "target")
}
\arguments{
\item{results}{Computed values: a data frame with the key column (\code{by}) and a
\code{value} column, or a named numeric vector (names are keys).}

\item{targets}{Documented targets: a data frame with the key column (\code{by}),
\code{value} (the reported value), \code{kind} (tolerance class), and optionally a
per-row \code{tolerance} override.}

\item{tolerances}{Optional named list/vector mapping \code{kind} to a numeric
tolerance; merged over (and so can extend or override) the defaults above.}

\item{by}{Name of the key column joining \code{results} to \code{targets}
(default \code{"target"}).}
}
\value{
A data frame, one row per target (in \code{targets} order), with columns
\verb{<by>}, \code{kind}, \code{reported}, \code{computed}, \code{diff}, \code{tolerance}, and \code{status}
(\code{"PASS"}, \code{"FAIL"}, or \code{"UNMATCHED"}). \code{diff} and \code{tolerance} are \code{NA} for
p-value rows.
}
\description{
The mechanical half of a replication or reproducibility check: join each
computed value onto its target, take the signed difference, and mark
\code{PASS} / \code{FAIL} / \code{UNMATCHED} against the tolerance for that target's \code{kind}.
It returns the comparison \emph{facts} only — the overall verdict vocabulary
(e.g. REPLICATED / PARTIAL / FAILED for a replication, PASS / FAIL for a
manuscript audit) is the calling skill's judgment, not this function's.
}
\details{
Tolerance for a row is resolved in order: an explicit non-\code{NA} \code{tolerance}
column on \code{targets} (absolute: \code{abs(computed - reported) <= tolerance});
otherwise the entry in \code{tolerances} for the row's \code{kind}. A \code{kind} of
\code{"p_value"} is compared by significance level (same bucket across the
conventional 0.001 / 0.01 / 0.05 cutpoints), not by numeric difference —
encode p-values as their actual value where known.

Defaults mirror the replication tolerance table (integers exact; estimates
\verb{< 0.01}; SEs \verb{< 0.05}; percentages \verb{< 0.1} pp): \code{count = 0},
\code{estimate = 0.01}, \code{se = 0.05}, \code{percentage = 0.1}, \code{summary_stat = 0.01}.
}
\examples{
targets <- data.frame(
  target = c("att", "N"), value = c(-1.632, 2847),
  kind = c("estimate", "count")
)
compare_targets(c(att = -1.628, N = 2847), targets)
}
\seealso{
The \code{estimation-diagnostics} skill (its \code{references/replication.md}),
which homes this helper, and the \code{audit-reproducibility} skill, which uses
it to check manuscript claims against pipeline outputs.
}
