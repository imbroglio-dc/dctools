% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{join_audit}
\alias{join_audit}
\title{Join with a declared contract and an audit trail}
\usage{
join_audit(
  left,
  right,
  by,
  type = c("left", "inner", "right", "full"),
  relationship = NULL,
  unmatched = "drop",
  na_matches = "never",
  quiet = FALSE
)
}
\arguments{
\item{left, right}{Data frames to join.}

\item{by}{Character vector of key column name(s), present in both frames.}

\item{type}{Join type: \code{"left"} (default), \code{"inner"}, \code{"right"}, or \code{"full"}.}

\item{relationship, unmatched, na_matches}{Passed to the underlying dplyr join.
\code{relationship} (e.g. \code{"one-to-one"}, \code{"many-to-one"}) errors on violation;
\code{unmatched} defaults to \code{"drop"}; \code{na_matches} defaults to \code{"never"}.}

\item{quiet}{Logical; suppress the audit message (the trail is still attached).}
}
\value{
The joined data frame, with a one-row audit data frame attached as
\code{attr(x, "join_audit")} (columns \code{type}, \code{by}, \code{left_n}, \code{right_n},
\code{joined_n}, \code{matched_left}, \code{unmatched_left}, \code{unmatched_right},
\code{match_rate}).
}
\description{
A strict-join wrapper: runs a dplyr join with the \code{relationship} /
\code{unmatched} / \code{na_matches} contract set explicitly, and returns the joined
data with a before/after row-count and match-rate audit trail attached. It
turns the merge-time failure \code{\link[=check_unique_id]{check_unique_id()}} cannot see — a join that
should be 1:1 but silently inflates rows — into a reported number, or, if you
declare \code{relationship}, an immediate error.
}
\details{
\code{na_matches} defaults to \code{"never"} (safer than dplyr's \code{"na"}): two rows with
\code{NA} keys are not the same entity. For a \code{left}/\code{inner} join, a grown row
count is flagged as a warning — the usual sign of duplicate keys on the right.
}
\examples{
left <- data.frame(id = 1:3, x = c("a", "b", "c"))
right <- data.frame(id = 1:2, y = c(10, 20))
j <- join_audit(left, right, by = "id")
attr(j, "join_audit")
}
\seealso{
\code{\link[=check_unique_id]{check_unique_id()}} for the single-table key check.
}
