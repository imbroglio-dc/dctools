% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{configure_parallel}
\alias{configure_parallel}
\title{Configure a local crew parallel controller}
\usage{
configure_parallel(
  max_workers = parallel::detectCores() - 1,
  memory_per_worker = 4,
  name = "dctools_local"
)
}
\arguments{
\item{max_workers}{Maximum workers to spawn (default \code{detectCores() - 1}).}

\item{memory_per_worker}{Target RAM per worker, in GB (default 4).}

\item{name}{Controller name (default \code{"dctools_local"}).}
}
\value{
A \code{crew_controller_local} object for use in a \code{targets} pipeline.
}
\description{
Creates a \code{crew::crew_controller_local()} capped by both the requested
worker count and available system RAM divided by \code{memory_per_worker}, so a
pipeline does not oversubscribe memory. Requires the \code{crew} and \code{ps}
packages.
}
